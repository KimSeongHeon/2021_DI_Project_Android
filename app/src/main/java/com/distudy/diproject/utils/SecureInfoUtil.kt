package com.distudy.diproject.utils

import com.distudy.diproject.BuildConfig

object SecureInfoUtil {
    const val OAuth_ID = BuildConfig.OAUTH_ID
    const val OAuth_SECRET = BuildConfig.OAUTH_SECRET
}